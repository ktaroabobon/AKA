{
  "name": "bot",
  "version": "0.1.0",
  "private": true,
  "description": "AKA LINE Bot (Google Apps Script, TypeScript)",
  "scripts": {
    "build": "tsx build.ts",
    "push": "pnpm build && clasp push",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
